# omg:autopilot

Autonomous execution — takes a task from idea to verified completion. Plans when needed, implements, verifies with evidence, iterates until done. Say 'ralph' for strict completion mode.

## Synopsis

```bash
copilot --agent omg:autopilot -p "describe your role" -s --yolo
copilot -i "@omg:autopilot analyze this code"
```

## Description

Autonomous execution — takes a task from idea to verified completion. Plans when needed, implements, verifies with evidence, iterates until done. Say 'ralph' for strict completion mode.

## Model

`claude-sonnet-4.6`

## Tools

`view,grep,glob,task,store_memory,report_intent`

## Example

```bash
copilot --agent omg:autopilot -p "describe your role in one sentence" -s --yolo
```

## Related

See [all agents](../readme.md) for the full catalog.
